const NUMERO_DE_CARTAS = 20;
export default () => {
     const FontAwesomeClasses = ['icon-2', 'icon-3']
     let cartas = []:
}
while (cartas.lenght < NUMERO_DE_CARTAS){
    const index = Math.floor = Math.floor(Math.random() * FontAwesomeClasses)
}
